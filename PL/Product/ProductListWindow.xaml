<Window x:Class="PL.Product.ProductList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Product"
        mc:Ignorable="d"
        Title="ProductListWindow" Height="640" Width="800">
    <Grid Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="500*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Name="UpGrid" Grid.Row="0" Width="auto" Margin="0,0,0,476" Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions >
            <Label Grid.Column="1" Content="FILTR BY CATEGORY" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="122,60,0,0" FontWeight="Bold"/>
            <ComboBox x:Name ="FilterProducts" HorizontalAlignment="Left" VerticalAlignment="Top"
               Grid.Column="1" Width="100" SelectionChanged="FilterProductsClick" Margin="258,63,0,0"/>
            <Label Content="Product List" HorizontalAlignment="Left" Margin="32,10,0,0" VerticalAlignment="Top" Width="153" FontWeight="Bold" FontSize="20" FontFamily="Cooper Black"/>
        </Grid>
        <ListView x:Name ="ProductListview" Cursor="Hand" MouseDoubleClick="AccessUpdateProduct" Margin="0,67,0,0" Grid.Row="1" >
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding ProductID}" Header="ID" Width ="100" />
                    <GridViewColumn DisplayMemberBinding="{Binding ProductName}" Header="Name" Width ="100" />
                    <GridViewColumn DisplayMemberBinding="{Binding ProductPrice}" Header="Price"   Width ="100"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Category}" Header="Category" Width ="100" />
                </GridView>
            </ListView.View>
        </ListView>
        <Button x:Name = "AddProduct" Cursor="Hand" Content="Add new Product" Grid.Row="2" HorizontalAlignment="Right" Margin="5" Padding="5" Click="AccessAddProduct">
            <Button.Resources>
                <Style TargetType="Button">
                    <Style.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5" />
                        </Style>
                    </Style.Resources>
                </Style>
            </Button.Resources>
        </Button>
        <Button x:Name = "AllCatgory" Cursor="Hand" Content="All Category" HorizontalAlignment="Left" Margin="5" Padding="5" Grid.Row="2" VerticalAlignment="Top" Click = "AllCatgoryClick">
            <Button.Resources>
                <Style TargetType="Button">
                    <Style.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5" />
                        </Style>
                    </Style.Resources>
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Window>
