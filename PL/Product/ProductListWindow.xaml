<Window x:Class="PL.Product.ProductList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Product"
        DataContext="{Binding RelativeSource={RelativeSource self}}"
        mc:Ignorable="d"
        Title="ProductListWindow" Height="640" Width="800">
    <Grid Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="40*" />
            <RowDefinition Height="500*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Name="UpGrid" Grid.Row="0" Width="auto" Margin="0,0,0,476" Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Grid.Column="1" Content="FILTR BY CATEGORY" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="122,60,0,0" FontWeight="Bold" />
            <ComboBox x:Name ="FilterProducts" HorizontalAlignment="Left" VerticalAlignment="Top"
               Grid.Column="1" Width="100" SelectionChanged="FilterProductsClick" Margin="258,63,0,0" />
            <Label Content="Product List" HorizontalAlignment="Left" Margin="32,10,0,0" VerticalAlignment="Top" Width="153" FontWeight="Bold" FontSize="20" FontFamily="Cooper Black" />
        </Grid>
        <ListView x:Name ="ProductListview" ItemsSource="{Binding productList}" Cursor="Hand" MouseDoubleClick="AccessUpdateProduct" Margin="0,67,0,0" Grid.Row="1">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding ProductID}" Header="ID" Width ="200" />
                    <GridViewColumn DisplayMemberBinding="{Binding ProductName}" Header="Name" Width ="200" />
                    <GridViewColumn DisplayMemberBinding="{Binding ProductPrice}" Header="Price"   Width ="200" />
                    <GridViewColumn DisplayMemberBinding="{Binding Category}" Header="Category" Width ="200" />
                </GridView>
            </ListView.View>
        </ListView>


        <Button x:Name = "AddProduct" Content="Add New Product"
                HorizontalAlignment="Right" Margin="5"
                Grid.Row="2" Click = "AccessAddProduct"/>
        
        <Button x:Name = "AllCatgory" Content="All Category"
                HorizontalAlignment="Left" Margin="5"
                Grid.Row="2" Click = "AllCatgoryClick"/>
    </Grid>
</Window>